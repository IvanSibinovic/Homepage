var person = {
    firstName:"John",  
    age:50, 
    eyeColor:"blue"
};

var noviRed = document.createElement("table tr");
var node = document.createTextNode("prvi");
para.appendChild(node);
var element = document.getElementById("tabela");
element.appendChild(para);